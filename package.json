{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:stencil": "lerna run build:stencil --scope @technway/stencil-library",
    "build:react": "lerna run build --scope @technway/react-library",
    "build:next": "lerna run build --scope @technway/next-library",
    "build:all": "npm run build:stencil && npm run build:react && npm run build:next"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.4.1",
    "@commitlint/config-conventional": "^19.4.1",
    "@types/node": "^22.10.1",
    "husky": "^9.1.7",
    "lerna": "^8.1.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "root": "file:"
  },
  "version": "3.0.2"
}