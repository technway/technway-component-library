// import { GLOBAL_PREFIX } from "../../utils/utils";

// const baseClass = `${GLOBAL_PREFIX}-navbar-menu`;

export const styles = `
* {
    box-sizing: border-box;
}
    
:host {
    display: grid;
}
`;